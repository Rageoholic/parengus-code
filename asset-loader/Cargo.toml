[package]
name = "asset-loader"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
asset-shared = { path = "../asset-shared" }
lz4_flex = { version = "0.12", features = ["frame"] }
serde = { version = "1", features = ["derive"] }
toml = "1"
